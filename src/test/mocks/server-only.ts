/*
 * Next.js-এর "server-only" package production-এ
 * server/client boundary enforce করে।
 *
 * Vitest-এর Node environment-এ server modules
 * unit-test করার জন্য এটি intentional empty mock।
 */

export {};